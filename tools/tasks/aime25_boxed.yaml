# AIME 2025 (30 problems), boxed answer, no stop strings.
# The reasoning canary: a miscalibrated quantization loops or rambles here long
# before it loses knowledge, which shows up as [invalid] (no answer in budget).
task: aime25_boxed
dataset_path: math-ai/aime25
test_split: test
output_type: generate_until
doc_to_text: !function utils.aime_doc_to_text
doc_to_target: "{{answer}}"
num_fewshot: 0
generation_kwargs:
  until: []
filter_list:
  - name: boxed
    filter:
      - function: regex
        regex_pattern: '\\boxed\{(\d+)\}'
        group_select: -1
      - function: take_first
metric_list:
  - metric: exact_match
    aggregation: mean
    higher_is_better: true
metadata:
  version: 1.0
